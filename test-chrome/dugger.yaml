# DuggerLinkTools Project Configuration
# Generated by DuggerBootTools for Chrome Extension

project:
  name: "test-chrome"
  version: "0.1.0"
  description: "test-chrome - Chrome Extension with DLT DNA validation"
  template_type: "chrome-extension"
  created_at: "TBD"
  
metadata:
  author: "PyPro"
  email: "pypro@dugger.com"
  license: "MIT"
  language: "javascript"
  
dependencies:
  core:
    - "duggerlink-tools"
  dev:
    - "webpack"
    - "webpack-cli"
    - "copy-webpack-plugin"
    - "eslint"
    - "prettier"
    - "jest"
  
structure:
  src_dir: "src"
  tests_dir: "tests"
  docs_dir: "docs"
  
chrome:
  manifest_version: 3
  permissions:
    minimal: ["storage", "activeTab"]
    optional: ["scripting", "tabs"]
  host_permissions: ["https://*/*"]
  
git:
  auto_init: true
  initial_commit_message: "chore: bootstrap test-chrome Chrome extension"
  
quality:
  linter: "eslint"
  formatter: "prettier"
  test_runner: "jest"
  
ide_sync:
  enabled: true
  chrome_devtools: true
  javascript_support: true